# 监控提示词

## 启动

启动为一个系统托盘图标，图标为statistics.ico，该应用一般情况下不会退出。
图标包含三个Menuitem，今天, 过去7天、选项、退出

- 启动后，启动一个http服务端口号为8000
  - '/api/dates/'
    获取当前目录的日期列表，由于日期分散于各个YYYYMM文件夹，需要遍历各文件夹
  - '/api/data/YYYYMMDD'
    YYYYMMDD由请求传入所需日期，如果日期不存在则返回{"sessions": [], "idle":0, "apps": {}}

- 今天 点击后，使用默认浏览器打开localhost:8000/daily.html
- 过去7天 点击后，使用默认浏览器打开localhost:8000/weekly.html
- 选项包含一个子选项，选项为”当日用户首次登陆“(userFirstLogin),"当日开机"(powerOn), 该选项保存于statistics.configuration.json，启动后会加载该配置，选中项会被勾选
- 退出 点击后，退出当前应用

### 创建文件夹、文件的线程

- 启动程序后，如果没有YYYYMM文件夹则创建
  - 如果YYYYMM目录下不包含YYYYMMDD.log, YYYYMMDD.app.json, YYYYMMDD.idle.json, YYYYMMDD.report.txt则创建
  - 等待电源、用户事件。
  - 等待活动窗体变化事件
  - 空闲变化事件
  - 跨天

#### 记录电源、用户事件

- 当电源事件发生后，写入YYYYMM\YYYYMMDD.log，例如开机、关机、休眠、睡眠、用户登录，用户登出、用户锁屏，用户退出应用等各种关键信息。
  - 当登出、锁屏、关机、休眠等事件发生时，立即获取活动窗体
  - 以下部分的逻辑和获取到活动窗体变化的逻辑一致
    - 根据currentProcess，和currentTitle，查找已存在对应的字典
      - 将当前时间写入ended
    - 将currentProcess，currentTitle设置为新的进程名、窗体名

#### 等待活动窗体变化事件

- 登录后监控当前活动窗体变化
- 首次运行时，立即获取活动窗体，
  - 根据窗体获取到对应的进程名
  - 根据窗体获取窗口名
  - 首次只记录started
- 记录进程名为currentProcess, title为currentTitle
- 记录如下数据
- 向YYYYMMDD.app.json写入数据

```json
{
  "msedge.exe": {
    "titles": {
      "Cocos Creator | SpatialperceptionTraining 和另外 6 个页面 - 用户配置 1 - Microsoft​ Edge": [
        {
          "started": "2026-03-29 12:58:40.123",
          "ended": "2026-03-29 13:28:20.789"
        }
      ]
    }
  }
}
```

- 当获取到变化时
  - 根据currentProcess，和currentTitle，查找已存在对应的字典，将当前时间写入ended
- 根据当前活动窗体，设置currentProcess, currentTitle，并向值添加started时间
- 向YYYYMMDD.app.json写入数据

#### 空闲变化

- 监控系统是否空闲，空闲的定义为无鼠标、键盘输入超过60秒
- 当空闲超过60秒后，将时间写入YYYYMMDD.idle.json
- 该监控只向idle中添加start，end由活动窗体变化事件填入
- 格式为

```json
[
  {
    "start": "2026-02-05 13:55:17.234",
    "end": "2026-02-05 13:55:48.345"
  }
]
```

#### 跨天

- 有时候应用程序不会退出，例如休眠、睡眠等情况。
  - 当监控程序被唤醒时，需要生成前一天的报告
    - 根据前一天的YYYYMMDD.app.json生成前一天的YYYYMMDD.report.json
  - 生成格式

  ```json
  {
    "sessions": ["2026-02-02 18:06:48", "2026-02-02 18:46:57"],
    "idle": 1000,
    "apps": {
      "msedge.exe": {
        "total": 234,
        "titles": {
          "Zen Balance and 2 more pages - Profile 1 - Microsoft​ Edge": 123
        }
      }
    }
  }
  ```

      的123为YYYYMMDD.app.json的end-start的总和
      total为所有的titles所有的值的总和
      sessions的时间根据第一条记录的时间和最后一条记录的时间

  idle为end-start的总和
  - 当监控程序被唤醒时，需要
    创建YYYYMM的文件夹
    创建YYYYMMDD.log
    创建YYYYMMDD.idle.json
    创建YYYYMMDD.app.json

## 网页

- daily.html与weekly.html
  - 参照daily.html和weekly.html, 但用echarts替换chart.js
  - 需要以目前的数据格式来实现
